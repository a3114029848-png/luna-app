# Luna AI 成本与延迟估算

> 用途：展示 AI 产品经理的成本 / 工程意识 —— 回答「AI 功能要烧多少钱、延迟能不能接受」。
> 版本：v1.1（2026-08-31，对齐：服务端代理 / 服务器成本 / 多人使用成本）
> ⚠️ 定价为公开市场量级估算【推断】，具体以所选模型的官方 API 定价为准。本地工具与知识库为 0 token 成本，是本文核心结论。

---

## 一、结论先行

**Luna 的三级架构让「AI 很便宜」**：高频问题（周期分析、阶段、指标、就诊摘要、知识库科普）全部在本地完成，**只有未覆盖的开放问答才产生 token 成本**。按 70% 问题走本地估算，云端成本占比可控制在较低水平。

---

## 二、各能力成本与延迟预算

| 能力 | Token 成本 | 延迟目标 | 依赖 | 降级策略 |
|---|---|---|---|---|
| 危险问题兜底 | **0** | <50ms | 本地关键词规则 | 无（必达） |
| 周期/阶段/指标分析 | **0** | <100ms | 本地规则引擎 + 记录 | 无网可用 |
| 就诊摘要 | **0** | <100ms | 本地汇总 | 无网可用 |
| 知识库科普 | **0** | <50ms | 本地检索 | 无网可用 |
| 开放问答（云端） | **有** | 首字 <1.5s，全答 <8s | LLM API | 失败 → 降级提示 |

> 关键点：**核心价值功能（数据类 + 科普类）离线可跑、零成本**，云端只是补充。

---

## 三、云端单轮成本估算（开放问答场景）

实际选用 **DeepSeek-chat**（`deepseek-chat`，2026-08-31 已通过服务端代理接入），公开价格量级：输入 ~¥1/1M tokens、输出 ~¥2/1M tokens（约 $0.14 / $0.28 量级，比 GPT-4o-mini 档更低【推断，以官方定价为准】）：

| 场景 | 平均输入 tokens | 平均输出 tokens | 单轮估算成本 |
|---|---|---|---|
| 简短问答（如「痛经还能吃止痛药吗」） | ~300 | ~150 | ~$0.00014 |
| 一般咨询（需解释 + 免责） | ~600 | ~300 | ~$0.00027 |
| 长对话/多轮（10 轮会话） | ~4000 | ~2500 | ~$0.0021 |

**月成本推演（示例）**：
- 假设 10,000 活跃用户，每月每人 30 次问答，其中 **30% 走云端**（本地命中率 70%）
- 云端调用 = 10,000 × 30 × 30% = 90,000 次/月
- 平均单轮 ~$0.0002 → 月成本 ≈ **$18 / 月**

> 结论：云端成本可忽略；真正的大头是「本地功能开发」而非「LLM 调用费」。

---

## 四、缓存与成本优化策略

1. **本地优先是最大「缓存」**：工具 + 知识库天然 0 成本、0 延迟，且不泄露隐私。
2. **热门 FAQ 静态化**：本地知识库 12 条 + **服务端 RAG 21 条权威条目**（2026-08-31），把高频开放问题「平移」成 0 token 回答；未命中才走云端。
3. **反馈驱动优化**：用户 👎 高频的问题 → 补知识库条目 → 减少云端调用（数据飞轮 + 降本双赢）。
4. **流式 + 提前终止**：云端用流式输出，首字即反馈；必要时按长度上限截断，防失控 token。

---

## 五、服务器与运维成本（2026-08-31 部署后新增）

**服务器**：腾讯云轻量 Windows Server 2022（2核2G）≈ **几十元/月**（新用户首年更低）。
- 承载：Express + SQLite(sql.js) + RAG + PDF + AI 代理，单机即可支撑小规模并发。
- 数据库 sql.js（WASM 零编译）无额外授权；RAG 用关键词×3 + bigram Jaccard，**零 embedding 成本**。

**多人使用成本（关键）**：分发给其他用户后，**所有 AI 流量都走你的 DeepSeek Key**（App 只连你的服务器）。
- 每人每日 10 次云端问答 ≈ 每次 ~¥0.002 → **每人月成本约 ¥0.6**；100 人 ≈ ¥60/月（纯 AI）+ 服务器 ¥几十/月
- **控制手段（建议实现）**：服务器按 userId 统计 AI 调用/token + **每日限额**（防刷防滥用）+ DeepSeek 后台余额告警

## 六、延迟预算与体验

| 环节 | 预算 | 说明 |
|---|---|---|
| 本地工具/知识库 | <100ms | 秒回，无感知 |
| 云端首字（TTFT） | <1.5s | 流式渲染，先出第一段 |
| 云端完整回答 | <8s | 超时给降级提示 |
| 无网/云端失败 | 即时降级 | 本地功能不受影响，提示重试 |

> 面试要点：**「本地功能不依赖网络」是体验与信任的双重保障**——即使用户断网，核心的健康分析仍可用，这也是隐私叙事的落地。

---

## 七、面试讲述要点

1. 先讲结论：**「我的 AI 大部分是免费的」**——靠三级架构把 LLM 调用降到最低。
2. 用上面的月成本推演展示成本意识（举一个数字即可，别背表格）。
3. 强调降级策略：断网也能用核心功能 → 隐私 + 体验 + 成本三赢。
4. 诚实标注：定价是量级估算，正式上线前按实际模型定价重算。
