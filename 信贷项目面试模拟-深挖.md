# 信贷项目面试模拟-深挖（第一轮）

> 用途：面试前的项目深挖演练材料——模拟面试官针对「个人消费信贷风险识别项目」的连环追问，每题给出「考察点 + 决策链参考回答 + 可能追问 + 可优化点」，最后评分卡与行动清单。
> 方法：基于 `project-interview` skill 的模拟面试模式（8 大类 A→H 出题，每问给考察点/参考回答/可优化点，一次 3-5 问，最后汇总评分）。
> 关联：`个人消费信贷风险项目-产品视角.md`（项目重述与弹药）｜`个人消费信贷需求分析.md`｜`个人消费信贷竞品分析.md`｜`项目面试问答知识库.md`（8 大类 + 6 公式）。
> ⚠️ 使用方式：每道题**先自己在脑子里答一遍**，再看参考回答对照——暴露真实短板比背稿有用。文中〔占位符〕处用你论文的真实指标填入。

---

## 使用前必读：评分五维（对照 skill）

| 维度 | 面试官通过什么判断 |
|---|---|
| 真实性 | 细节追问：特征怎么筛、PSI 阈值、踩过什么坑 |
| 决策力 | 有没有「为什么不用方案 B」的权衡过程 |
| 方法论 | 讲不讲得清「业务问题→特征→模型→评测→合规→飞轮」证据链 |
| 量化 | 数字能不能现场算（精确率/AUC→坏账→钱） |
| 边界诚实 | 主动暴露公开集/离线/未上线 + 下一步优先级 |

---

## 第 1 问（A 概述 · 开场）：「用三句话介绍一下你这个项目」

### 🎯 考察点
表达结构、是否产品视角（而非「我做了 Logit/SVM/RF」的功能清单）、是否抓得住重点。

### ✅ 参考回答（决策链）
> 「一句话业务：这是一个**贷前信用风险评估项目**——在放款前判断借款人会不会违约，帮机构在『多放款赚钱』和『少放错款亏钱』之间做权衡（X：先定义业务问题，而不是先讲模型）。
> 我的核心判断是：**特征质量决定模型上限，稳定比单点强更重要**——所以先做特征工程与筛选（IV + 稳定性 + 相关性），选出稳定特征集，再融合 Logit / SVM / 随机森林（Y：为什么这么做）。
> 结果：在可解释的前提下提升精确率与 AUC（Z），并明确知道它是离线验证、需真实数据再校验（诚实边界）。」

### 🔁 可能的追问链
- 「你说的『稳定』具体指什么？」→ 引到第 3 问（PSI / 跨时间分布）
- 「精确率多少、AUC 多少？」→ 填真实值，接第 4 问（翻译成钱）

### ⚠️ 可优化点
- ❌ 败笔：「我用了随机森林、SVM、逻辑回归做了个分类，AUC 0.9」——简历复读，不是产品介绍。
- 已做到「业务→判断→结果→边界」四段式，合格；时间紧可压成两句，保留「稳定特征」这个差异化记忆点。

---

## 第 2 问（C 边界取舍 · 最可能被追问）：「为什么用 Logit/SVM/RF 融合，而不用现在主流的 XGBoost / LightGBM / 深度模型？」

### 🎯 考察点
**决策力 + 诚实度**——算法项目的"送命题"，看你会不会慌、会不会吹牛。

### ✅ 参考回答（决策链）
> 「这是我有意做的取舍（X）。信贷场景有**三个约束**：监管要可解释、业务要效果、生产要稳定——单一模型满足不了，所以我选了『**可解释基线 + 效果模型融合**』的路线（Y）：
> - **Logit**：监管友好的可解释基线，作为融合成员和对照；
> - **随机森林/SVM**：补非线性区分能力。
> 我承认 XGBoost/LightGBM 单点效果可能更强（诚实承认），但它们可解释性更弱，真实授信场景需额外做解释层（如 SHAP）——**这恰恰证明『模型选型是业务约束驱动的权衡，不是堆最新技术』**（Z：结论升华）。」

### 🔁 可能的追问链
- 「你怎么验证融合真的比单个强？」→ 对照组 / 加权 vs 投票的实验对比（如实说）
- 「可解释到底怎么落地？」→ SHAP / 特征贡献 / Logit 系数业务化（可解释基线）
- 「为什么不用深度学习？」→ 数据量级、可解释、监管，三约束不匹配（与 XGBoost 同理）

### ⚠️ 可优化点
- 千万别贬低 XGBoost——正确姿势是**承认它强，但指出约束不匹配**。
- 加分：补一句「我也跑了 XGBoost 作对照，结论是……」——有对照组可信度翻倍。**没跑过就别说**（诚实边界）。

---

## 第 3 问（D 技术实现 · 真实性细节）：「样本不均衡你怎么处理？缺失值、类别特征呢？你说的『稳定特征』具体怎么定义？」

### 🎯 考察点
**真实性**（造假项目最怕细节连环问）+ 技术深度 + 是否真正动手跑过。

### ✅ 参考回答（决策链，分点）
> 「我按『先数据、后特征、再模型』的顺序处理（X）：
> - **样本不均衡**：先看违约占比——偏差大时用类别权重 / 采样（如 SMOTE），**同时用精确率-召回率而非单纯准确率评估**，因为准确率在失衡样本下会骗人（Y：指标口径）。
> - **缺失 / 类别**：缺失值按业务含义处理（如『收入缺失』本身可能就是风险信号，不只用均值填充）；类别特征编码后做 IV 筛选。
> - **稳定特征定义**：两层筛选——**预测力**（IV/相关性）+ **稳定性**（跨时间段 PSI，即特征分布漂移度）。单点区分度高但 PSI 恶化的特征排除，因为生产客群会漂移（Z：为什么这样做）。」

### 🔁 可能的追问链
- 「PSI 阈值设多少？」→ 如实答（通常 <0.1 稳定 / 0.1~0.25 需关注 / >0.25 恶化）
- 「SMOTE 后怎么防过拟合？」→ 只在训练集采样、交叉验证、看测试集泛化
- 「具体哪几个特征被排掉了、为什么？」→ 准备 1-2 个真实例子（真实细节最值钱）

### ⚠️ 可优化点
- 这题最容易暴露「是不是真跑过」——**细节务必按你论文真实做法改**，别照抄模板。
- 提前准备 1-2 个真实踩坑（如「某个高区分特征我怀疑生产会漂移，排掉了」）。

---

## 第 4 问（E/F 质量与算账 · 决胜题）：「AUC 从〔X〕到〔Y〕，对业务到底意味着什么？你这个模型能帮机构省多少钱？」

### 🎯 考察点
**把指标翻译成业务价值 + 算账能力**——AI PM 与纯算法的分水岭，本项目的核心卖点。

### ✅ 参考回答（决策链 + 算账）
> 「我把它翻译成三笔账（X：先定义业务口径）：
> - **AUC** 提升 = 排序区分度提升 = 同样通过率下，坏账率预期下降〔X〕个点（Y：指标→业务）；
> - **精确率** 提升 = 更少放错款 = 每万笔放款减少坏账约〔金额〕元（按单笔放款〔Y〕元、坏账损失率〔Z〕估算）；
> - 但这不是一味卡严——**最优切点在『边际坏账成本 = 边际利息收入』**，我会画『通过率-坏账率曲线』帮业务定切点，而不是只看 AUC 最大（Z：落到商业决策）。」

### 🔁 可能的追问链
- 「坏账损失率假设哪来的？」→ 如实：公开常识口径 / 待核验，标注边界
- 「AUC 0.85 是不是越高越好？」→ 不是，要和计算/解释/监控成本一起看（展示不指标至上）
- 「这个估算的置信度？」→ 诚实：基于估算口径，真实业务需实测

### ⚠️ 可优化点
- **全场最关键一问**。立刻把真实数字填进 `个人消费信贷风险项目-产品视角.md` 第九节，并预先算好「每万笔放款省多少钱」——**现场算得出数字 = 加分；卡壳 = 前面白讲**。
- 加分：补「AUC 不是越高越好」一句，展示成本意识。

---

## 第 5 问（G/H 复盘迁移 · 收尾）：「这项目在真实业务里能用吗？如果不能，它的价值在哪？对你做 AI 产品经理有什么用？」

### 🎯 考察点
**诚实边界 + 优先级思维 + 迁移能力**——判断你是否清楚自己的项目分量、会不会吹牛。

### ✅ 参考回答（诚实边界 + 迁移）
> 「**不能直接上线，我明确知道**（诚实）：数据是 Kaggle 公开集，非真实授信样本，没有真实逾期口径；评测是离线回测；『稳定特征』是统计口径的稳定，没做真实时间序列漂移验证。
> 但它的价值不在"上线"，而在**把风控决策闭环跑通**（X→Y）：业务问题定义 → 风险信号拆解 → 评测体系（精确率/AUC 对应坏账成本）→ 可解释与公平底线 → 数据飞轮（贷后表现回流重训）。
> 下一步优先级：①真实数据重验稳定特征 → ②上线后建 PSI 监控与重训机制 → ③切点策略 + AB 测试 → ④与额度定价联动（Z：PM 排序，不是功能清单）。
> 对 AI 产品经理：这套『需求→设计→评测→合规→数据飞轮』和我做 Luna 是同一套方法论，区别只在评测对象是 LLM 还是评分模型（迁移结论）。」

### 🔁 可能的追问链
- 「如果坏账率上线后上升了，第一步排查什么？」→ 数据/客群漂移（PSI）→ 特征失效 → 策略切点 → 外部环境（先数据后策略）
- 「给你一个团队，第一个月做什么？」→ 对齐业务口径 + 接真实数据 + 建监控，而不是堆模型

### ⚠️ 可优化点
- 千万别答「能上线」——会被追问到死。
- 「下一步优先级」背熟——主动暴露边界后**立刻接上价值锚点**，证明不是找借口而是有 PM 排序能力。

---

## 📊 本轮评分卡（对照五维）

| 维度 | 本轮表现 | 差距 |
|---|---|---|
| **真实性** | ⭐⭐⭐ | 需把第 3 问细节按论文真实做法对齐 |
| **决策力** | ⭐⭐⭐⭐ | 第 2 问融合逻辑成立，加对照组更稳 |
| **方法论** | ⭐⭐⭐⭐ | 决策链 + 数据飞轮贯穿，很好 |
| **量化** | ⭐⭐ | **最大短板**——占位符数字未填，第 4 问无法现场算账 |
| **边界诚实** | ⭐⭐⭐⭐ | 主动暴露边界，路线正确 |

## ✅ 行动清单（面试前必须做）

1. **填数字**：`个人消费信贷风险项目-产品视角.md` 第九节——精确率 / AUC / 召回率 / KS / PSI / 特征数
2. **预计算账**：按单笔放款额 × 坏账损失率，算出「每万笔放款省多少钱」
3. **补对照组**（可选）：XGBoost 对照结论，1 句话
4. **准备真实细节**：1-2 个被排除的特征 + 1 个真实踩坑（第 3 问用）
5. **背熟下一步优先级**：①真实数据 → ②PSI 监控 → ③切点 AB → ④定价联动

---

## 延伸：第二轮深挖候选问题（预告）

> 第一轮覆盖 A/C/D/E/F/G/H 核心，第二轮更刁钻，建议单独再演练一轮：

1. 「你这个精度提升有没有统计显著性？是不是调参调出来的？」
2. 「如果坏账率上线后上升了，你第一步排查什么？（排序题）」
3. 「你怎么向一个完全不懂技术的业务负责人解释这个模型？」
4. 「如果给你一个团队 + 真实数据，第一个月做什么、怎么定里程碑？」
5. 「用户反馈里『额度说降就降、被误伤无解释』，作为一个产品经理你怎么看这个矛盾？」
6. 「这个模型如果要用在你的 Luna 项目（非金融），方法论怎么平移？」

> 需要时可直接说「继续第二轮深挖」，按本轮同样格式（考察点 + 决策链回答 + 追问 + 可优化点）演练。
