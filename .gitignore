node_modules/
android/.gradle/
android/build/
android/app/build/
*.log
.expo/

# 环境变量（含密钥，禁止提交）
.env
server/.env

# 运行时数据 / 构建产物（不上传）
server/data/
*.db
*.db-shm
*.db-wal
android/app/.cxx/

# 个人求职文档（不上传公开仓库，本地保留；根目录 .md 默认忽略，白名单保留 README/skill）
/*.md
!/README.md
!/skill.md
