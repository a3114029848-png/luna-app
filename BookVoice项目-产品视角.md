# BookVoice 多智能体内容生成项目（产品经理视角重述）

> 用途：把「AI 多智能体内容生成项目（BookVoice）」从 AI 产品经理视角重新阐述，用于面试讲述与复盘。核心不是讲「我会用 Coze」，而是讲「**我能把人工创作流程拆解成 Agent 工作流并产品化，会用 Prompt 迭代管住生成质量，能用量化结果证明效率提升**」。
> 项目本质：基于 Coze 零代码搭建「输入解析-书籍推荐-金句生成-图片生成-素材合成」端到端自动化工作流，多次 Prompt Engineering 迭代优化生成质量与稳定性，将单条视频制作时长从约 3 小时缩短至 10 分钟内。
> 配套方法论：`项目面试问答知识库.md`（8 大类问题 + 6 公式 + 决策链）｜`Luna项目讲述框架.md`（决策地图叙事）。
> ⚠️ 诚实口径：本文「〔占位符〕」处为信息缺口，需用真实细节回填，**禁止编造**；文末附「信息缺口清单」，面试前务必补齐。
> 项目在简历中的相对位置：Luna 偏「AI 产品全链路」，本项目的独特价值是「**AI Agent 工作流编排 + 零代码快速验证**」，两者互补。

---

## 〇、30 秒定位句（开场先立框架，AI PM 视角）

> 「BookVoice 是我用 Coze 搭建的**多智能体内容生成工作流**。它解决的是『读书类视频制作成本高』的问题——我把创作流程拆成 5 个可编排的 Agent 节点，零代码跑通端到端自动化，把单条视频从约 3 小时压到 10 分钟内。
> 它证明的不是『我会用 Coze』，而是：**我能把人工创作流程拆解成 Agent 工作流并产品化、会用 Prompt 迭代管住生成质量、能用最小成本快速验证一个 AI 产品闭环是否成立**——这是我做 AI 产品经理的核心能力。」

---

## 一、业务问题定义（PM 视角的需求分析，替代「我搭了个工作流」）

### 1.1 一句话业务问题
> 读书 / 知识类视频制作链路长、成本高：选题 → 找书 → 提炼金句/观点 → 配图 → 剪辑合成，人工做一条通常要数小时【推断：3 小时为简历口径】。BookVoice 解决的是：**如何用 AI 工作流把这 5 个环节自动化，让人只提供最前端的"需求"和最末端的"审片"。**

### 1.2 目标用户与场景（⚠️ 推断，待补证据）
- 【推断】目标用户：做读书/知识内容的个人创作者、自媒体或内容机构
- 【推断】核心场景：把「一本书/一个主题」快速变成一条可发布的视频（脚本 + 金句 + 配图 + 合成素材）
- ⚠️ 简历未提供用户证据，面试前需补充「为什么这些人需要 / 有没有实际用过的人」

### 1.3 本项目的产品定位（PM 视角）
- 它不是"上线一个产品"，而是**验证「内容创作 → Agent 工作流」的产品化闭环是否成立**
- 它是一个**可复用、可迁移的工作流方法论**——拆解 + 编排 + 迭代的方法，不止用于读书视频

---

## 二、工作流拆解 = 产品设计（PM 视角，本项目的核心价值）

> 面试官问「你这个项目产品化在哪」，就讲这里：**把一条人工创作流程，翻译成 5 个可编排、可单独迭代的 Agent 节点。**

| 节点 | 用户任务（业务语义） | Agent 职责 | 质量约束（Prompt 方向） |
|---|---|---|---|
| ① 输入解析 | 用户想要什么内容 | 理解需求（主题/书目/风格） | 解析结果结构化、不跑偏 |
| ② 书籍推荐 | 讲哪本书 | 匹配主题推荐书单 | 相关性、经典性 |
| ③ 金句生成 | 内容的核心亮点 | 提炼打动人的金句/观点 | 忠于原著、**必须引用书中原句（迭代后）** |
| ④ 图片生成 | 配什么图 | 生成/匹配配图 | 风格统一、与内容匹配 |
| ⑤ 素材合成 | 怎么变成一条视频 | 组合脚本/图/字幕/配音为素材 | 节奏、可发布性 |

> **PM 视角**：这就是**Agent 产品设计**——流程拆解（每个节点负责什么）、边界划分（节点间传什么）、质量约束（每个节点怎么管质量）。和 Luna 的「意图识别 + 4 个工具 + 兜底规则」是同一套能力，只是形态从「App 内对话」换成了「内容工作流」。

### 2.1 模型选型（已确认）
| 节点类型 | 模型 | 说明 |
|---|---|---|
| 文本类（输入解析 / 书籍推荐 / 金句生成） | **DeepSeek** | 文本理解与生成，负责解析、推荐、金句 |
| 图片生成 | **豆包** | 负责配图生成 |
| 编排 / 合成 | Coze 工作流 | 节点串联、参数传递、素材合成 |

> **PM 视角**：模型选型本身也是一次判断——**文本任务用 DeepSeek（文本能力强、成本可控），图像任务用豆包（多模态生成），各节点选最合适的模型，而不是全部用一个**。这体现「按任务类型选模型」的成本与效果意识（呼应 Luna 的「能确定就不用 AI」的同类思维：**不迷信单一模型，按任务分工**）。

---

## 三、核心判断（决策链 X→Y→Z，面试必讲）

### 判断 1：流程可拆解 → 拆成 5 个节点，各司其职
> 「我判断**内容创作是可拆解的确定性流水线**（X）——所以把整条链路拆成『解析→推荐→金句→配图→合成』5 个节点，每个节点一个 Agent、一份 Prompt、可单独迭代（Y）——端到端自动化跑通，人只碰首尾两端（Z）。」

### 判断 2：验证期选 Coze 零代码 → 用最小成本跑闭环
> 「我判断**验证阶段最重要的是低成本快速闭环**（X）——所以选 Coze 零代码编排而非自研工作流引擎（Y）——1 个月内跑通产品化闭环，用最小成本验证『AI 自动做视频』成立（Z）。」

### 判断 3：Prompt 迭代管质量 → 生成质量与稳定性收敛
> 「我判断**多智能体生成质量不稳定是最大风险**（X）——所以把『优化质量与稳定性』作为迭代主线，多次 Prompt Engineering 收敛坏输出（Y）——最终达到可复用的稳定闭环（Z）。」

> **和 Luna 的连接**：Luna 用「规则 + 评测集」管质量，这里用「Prompt 迭代」管质量——**前者是强约束，后者是轻约束；方法同构，深度不同**（诚实说明差异，反而体现方法论层次）。

---

## 四、质量与迭代（E 类）——⚠️ 待补量化口径

- 简历明确：**多次 Prompt Engineering 迭代**优化"生成质量与稳定性"
- 可讲的方向（决策链）：定义质量标准 → 发现坏输出（如金句跑题/配图不匹配）→ 改 Prompt 约束 → 回归验证
- **信息缺口**：当前没有量化评测口径（如"金句通过率""生成成功率"）——面试被问「怎么衡量"稳定"」时必须能答，否则只能用主观描述

### 4.1 具体迭代案例：金句生成跑题（真实复盘，G 类必讲）
- **现象**：金句生成最初经常跑题——例如输入《月亮与六便士》，生成的是泛泛的"人生感悟/鸡汤"，而不是书里真正的话，和书的内容脱节
- **迭代动作**：在 Prompt 里加硬约束「**必须引用书中原句**」，并要求金句能落到具体章节/原话
- **结果**：金句更贴合原著、更有"这本书的味道"，跑题明显减少
- **PM 视角（决策链）**：发现坏输出（金句跑题，X）→ 判断问题在"要求模糊"而非模型能力（Y）→ 把模糊要求（"生成好金句"）改成可执行约束（"引用书中原句"），用规则锁质量（Z）。**这和刘 Luna 用「规则固话边界」管质量是同一种思维——只是这里用 Prompt 做轻约束。**

---

## 五、成本与效率（F 类）

| 维度 | 已知 | 待补 |
|---|---|---|
| 制作时长 | 约 3 小时 → 10 分钟内（约 **18 倍**效率提升） | — |
| 实现方式 | Coze 零代码（无自研开发成本） | — |
| 单条生成成本 | — | 〔Coze 单条 token/调用成本〕 |
| 一次通过率 | — | 〔生成成功率 / 返工率〕 |

> **PM 算账方向**：若补上"单条成本"，可算「相比人工 3 小时，10 分钟成本是多少、规模化后边际成本趋近多少」——把效率提升讲成钱（呼应知识库 F 类）。

---

## 六、与 AI 产品经理岗位的连接（H 类，为什么这个轻项目也值得讲）

| 能力 | 本项目证据 | 迁移到 AI PM 岗位 |
|---|---|---|
| **Agent 工作流编排** | 5 节点拆解 + 边界 + 传参 | 设计 Agent 产品（流程拆解是通用能力） |
| **Prompt Engineering** | 多次迭代管质量与稳定性 | 管 LLM 输出质量（任何 AI 产品都要） |
| **快速验证 MVP** | 零代码 1 个月跑通闭环 | 低成本验证产品假设的 owner 意识 |
| **量化成果** | 3h → 10min | 把效率提升数字化表达 |

> **一句话**：Luna 证明我会"做 AI 产品全链路"，BookVoice 证明我会"用 AI Agent 工作流解决内容生产问题"——两者合起来，覆盖了「产品」和「工作流/Agent」两个 AI PM 的关键面。

---

## 七、诚实边界与下一步（G 类复盘，主动讲反而加分）

### 7.1 已知边界（先说清）
- **无真实用户验证**：目标用户是【推断】，没有实际使用反馈
- **无量化评测集**：质量"稳定"缺少指标口径，更多靠主观迭代
- **未上线/未规模验证**：跑通的是工作流闭环，不是带用户和数据的成熟产品
- **平台依赖**：基于 Coze 平台能力，自研落地需迁移【推断】

### 7.2 下一步优先级（PM 排序，不是功能清单）
> ① 明确**目标用户与场景**（访谈创作者，确认"自动生成视频"是否真实刚需）→ ② 建立**量化质量口径**（金句通过率 / 一次成功率）→ ③ 把"稳定"从主观变成可度量 → ④ 规模化前先算清**单条成本账**。这个排序的逻辑是：**先验证需求与质量，再谈规模化与成本。**

---

## 八、面试高频问答弹药（按知识库 8 大类，决策链表述）

### A 概述
**Q：介绍一下这个项目。**
> 「用 Coze 零代码搭建的多智能体内容生成工作流：输入解析→书籍推荐→金句生成→图片生成→素材合成，把读书视频制作从约 3 小时压到 10 分钟。它体现的是我把创作流程拆解成 Agent 工作流、并用 Prompt 迭代管质量的能力。」

### B 动机需求
**Q：为什么做这个 / 用户是谁？**
> 「读书类视频制作链路长、成本高，有自动化需求【推断，待补用户证据】。我做它，是想验证『人工创作流程 → Agent 工作流』的产品化闭环是否成立——这是 AI 内容产品的通用命题。」

### C 边界取舍
**Q：为什么用 Coze 不自己写？**
> 「验证期最重要的是低成本快速闭环（X）——Coze 零代码编排省去自研引擎的时间与成本（Y）——1 个月跑通闭环，用最小成本验证假设（Z）。如果进入规模化，再评估自研/平台迁移（诚实补充）。」

### D 技术实现
**Q：多智能体怎么协同、怎么传参？**
> 「5 个节点按创作工序串联，前节点输出结构化结果作为后节点输入（如：解析结果 → 推荐书单 → 金句文本 → 配图描述 → 合成素材），每个节点可单独改 Prompt 迭代。模型选型上，**文本类节点（输入解析/书籍推荐/金句生成）接 DeepSeek，图片生成接豆包，由 Coze 工作流统一编排**——按任务类型选最合适的模型，而不是全部用同一个【待补：插件与传参格式细节】。」

### E 质量评测
**Q：生成质量怎么衡量、怎么知道"稳定"了？**
> 「简历口径是多次 Prompt 迭代优化质量与稳定性。诚实说，我当时的量化口径不完整——如果重做，我会先定义金句通过率/一次成功率再迭代【信息缺口，面试前必须补】。」（诚实 + 改进 = 加分）

### F 成本资源
**Q：这个工作流会不会很贵？**
> 「Coze 平台托管、零自研成本；单条效率从 3 小时降到 10 分钟【单条 token 成本待补】。规模化时真正的成本在 LLM 调用与图片生成，需要算单条成本账。」

### G 复盘成长
**Q：最大的坑 / 重来一次改什么？**
> 「两个真实的坑：一是**金句生成最初常跑题**——比如输入《月亮与六便士》，生成的是泛泛的人生感悟而不是书里的话；我给 Prompt 加了硬约束『必须引用书中原句』后跑题明显减少。二是**质量缺乏量化口径**，'稳定'更多靠主观，重来一次我会先建评测标准（金句通过率/一次成功率）再迭代，而不是边试边改。这让我明白：**AI 内容生成的 PM 价值，一半在流程拆解，一半在质量度量。**」

### H 岗位迁移
**Q：这个项目和 Luna 是什么关系 / 对 AI PM 有什么用？**
> 「互补关系：Luna 证明我会做 AI 产品全链路（需求→评测→合规→飞轮），BookVoice 证明我会用 Agent 工作流解决内容生产、用零代码快速验证。两者叠加，覆盖 AI 产品经理在『产品』和『Agent/工作流』两个面的能力。」

---

## 九、量化成果表（诚实口径，填真实值）

| 维度 | 我的成果 |
|---|---|
| 工作流 | 5 节点端到端自动化（解析→推荐→金句→配图→合成） |
| 实现方式 | Coze 零代码 |
| 模型选型 | 文本类节点接 DeepSeek；图片生成接豆包；Coze 编排 |
| 制作时长 | 约 3 小时 → 10 分钟内（约 18 倍） |
| 迭代方式 | 多次 Prompt Engineering 迭代（〔N〕次） |
| 典型迭代案例 | 金句跑题 → Prompt 加「必须引用书中原句」（《月亮与六便士》） |
| 单条成本 | 〔待补：token/调用成本〕 |
| 生成成功率 | 〔待补：一次通过率〕 |
| 质量口径 | 〔待补：是否建立量化评测〕 |
| 方法论沉淀 | 流程拆解 + Prompt 迭代 + 零代码快速验证，可迁移到任意内容生成/Agent 产品 |

---

## 十、信息缺口清单（面试前必须补齐，禁止编造）

| # | 缺口 | 为什么重要 |
|---|---|---|
| 1 | 目标用户与场景证据 | B 类"用户是谁"必考题，当前是【推断】 |
| 2 | 5 个节点具体实现（模型/插件/传参） | **部分已补**：文本类接 DeepSeek、图片生成接豆包；插件与传参格式细节待补 |
| 3 | 质量量化口径（通过率/成功率/返工率） | E 类必考题，没有数字会显得"稳定"是空话 |
| 4 | 1 个具体迭代翻车案例（哪环节差、改了什么、怎么验证） | **已补**：金句跑题 → Prompt 加「必须引用书中原句」（案例：《月亮与六便士》） |
| 5 | 单条成本与一次通过率 | F 类算账，把效率提升讲成钱 |

> 补齐方式：回忆你当时真实的 Coze 配置、Prompt 迭代次数、翻车案例，能填多少填多少；填不了的面试前不要主动展开，被问到诚实说"当时未量化，重做会先建口径"。

---

## 十一、一句话收束（面试结尾用）

> 「BookVoice 最初在我眼里是『搭了个 Coze 工作流』，现在我用产品视角重新理解它：**它是我把一条人工创作流程，翻译成可编排、可迭代、可度量的 Agent 工作流的完整练习。** 对一个 AI 产品经理来说，能把流程拆成节点、能把质量从主观变可测、能用最小成本验证闭环——这三件事，比『会用某个平台』重要得多。」
