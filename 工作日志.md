# Luna 项目工作日志（2026-08-24/25）

> 本文件用于记录本次工作，下次打开项目时据此回顾。
> 相关：`需求分析.md`、`竞品分析.md`、`AI评测集.md`、`AI产品设计.md`、`AI成本估算.md`、`面试演示脚本.md`、`简历项目经历.md`、`智能耳机竞品分析.md`

---

## 一、今天做了什么（概览）

| # | 事项 | 产出/结果 |
|---|---|---|
| 1 | 需求分析（五步法） | `需求分析.md` |
| 2 | 竞品分析（五步法，Flo/Clue/美柚/大姨妈） | `竞品分析.md` |
| 3 | 竞品分析 Skill 沉淀 | `skill.md` + 已迁移到 `.copilot/skills/` 与 `.github/skills/` |
| 4 | AI 能力落地（P0+P1+P2） | `agentTools.js` / `medicalKB.js` / `feedbackStore.js` / `AIScreen.js` 改造 |
| 5 | AI 方法论文档 | `AI评测集.md` / `AI产品设计.md` / `AI成本估算.md` |
| 6 | DeepSeek 云端跑通（含真机踩坑） | `api.js` 的 `deepseekChatStream`（XHR 流式） |
| 7 | Android SDK 安装 + 真机部署 | 本机装 SDK；`npm run android` 构建成功（真机 RMX3161） |
| 8 | 面试作品包 | `面试演示脚本.md` / `简历项目经历.md` |
| 9 | Skill 触发验证 + 智能耳机竞品分析 | `智能耳机竞品分析.md` |
| 10 | Git 上传（进行中，有安全处理） | ⚠️ 见「五、Git 待办」 |

---

## 二、核心业务事实（写代码/做判断前先看）

- **定位**：个人经期健康管理 App，差异化 =「纯本地隐私 + FIGO 医学规则 + 可溯源 AI + 就医衔接」
- **北极星指标**：周连续记录率（过去 7 天记录≥3 天的活跃用户 / 7 天活跃用户）
- **医学规则**：`src/constants/medicalThresholds.js`（FIGO：周期 24~38 天、时长≤8、标准差≤9、连续 2 期触发预警）
- **AI 三级架构**：
  1. 本地工具层 `agentTools.js`（4 工具：周期/阶段/指标/就诊摘要，调 FIGO 规则引擎）
  2. 本地知识库 `medicalKB.js`（12 条带来源条目，关键词检索 `searchKB`）
  3. 云端 DeepSeek `api.js` 的 `deepseekChatStream`（XHR 流式 SSE）
  - 危险/诊断类问题 **100% 走安全兜底**（`DANGEROUS_KEYWORDS`），不交 LLM
- **评测**：`AI评测集.md`（20+ 用例，工具命中≥90%、幻觉≤5%、危险兜底=100%）
- **反馈闭环**：`feedbackStore.js`（👍/👎 → AsyncStorage → 坏样本回流）

---

## 三、关键技术踩坑（重要，别再踩）

1. **React Native 的 fetch 不支持 `res.body.getReader()`**（body 是 undefined）→ 真机必报 `getReader of undefined`
   - ✅ 解决：`deepseekChatStream` 改用 **XMLHttpRequest**（`readyState>=3` 时增量读 `responseText` 解析 SSE）
   - ⚠️ 教训：AI/网络功能必须**真机验证**，不能只在 Node 里测
2. **本机原无 Android SDK**：`ANDROID_HOME` 曾指向不存在的路径
   - ✅ 已装到 `C:\Users\vincent\AppData\Local\Android\Sdk`（platform-tools + platforms;android-36 + build-tools;36.0.0）
   - adb 完整路径：`C:\Users\vincent\AppData\Local\Android\Sdk\platform-tools\adb.exe`
3. **C 盘满导致构建失败（磁盘空间不足）**
   - ✅ 解决：设置 **`GRADLE_USER_HOME=E:\gradle-home`**（E 盘 50.9GB 空闲），迁移 `.gradle` 缓存到 E 盘
   - ⚠️ 注意：**vscode-gradle 扩展会写坏 E:\gradle-home 的锁文件** → 若遇 `Unexpected lock protocol found in lock file (Expected 3, found 0)`，见仓库记忆 `gradle-build-issues.md`（根治：禁用 "Gradle for Java" 扩展）
4. **PowerShell 环境怪癖**：
   - 执行策略禁止 `.ps1` → 用 **`npm.cmd`** 而非 `npm`
   - 终端变量解析有 bug（`$x` 常报"无法识别"）→ 命令里用**字面完整路径**，别依赖变量
5. **真机 USB 调试 `offline`**：多为数据线/接口问题（换线/换口/手机解锁亮屏），软件重连只是辅助

---

## 四、环境备忘

- Node ≥22（本机 24.18）、JDK 17（Corretto，JAVA_HOME 已配）
- Gradle 9.3.1（wrapper）
- DeepSeek API Key：**只存在 `src/services/api.js`（demo 用，勿上架/勿提交 Git）**，现在文件里是占位符 `你的api密钥`
- 真机：RMX3161（realme）
- 远程仓库：`https://github.com/a3114029848-png/luna-app.git`（分支 main）

---

## 五、Git 状态（✅ 已完成 2026-08-25）

**已全部完成**：本地与 `origin/main` 同步（HEAD = `55260a2`，5 个提交），**已 push 成功**。
真实 DeepSeek Key 历史已清理（`git reset --soft origin/main` → 重新提交 `afeeae5` → 验证无 `sk-` → push）。

- [x] `git reset --soft origin/main`（撤销含 Key 的未推送提交）
- [x] 重新 `git commit`（干净版本，无 Key）
- [x] 验证无 `sk-` 残留
- [x] `git push origin main` 成功
- [x] HEAD 的 api.js 为占位符「你的api密钥」
- [ ] （可选）`工作日志.md` 尚未提交，可按需 push

> 备注：若未来再次误提交含 Key 版本且已 push，立即到 DeepSeek 平台更换 Key + `git filter-repo` 清洗历史。

---

## 六、面试作品包清单（已就绪）

- 文档：需求分析 / 竞品分析 / AI产品设计 / AI成本估算 / AI评测集 / 面试演示脚本 / 简历项目经历
- 代码：Agent 工具层 + 知识库 + 反馈闭环 + 安全兜底 + DeepSeek 云端（真机已跑通）
- Skill：`competitive-analysis`（个人级 + 项目级）
- 面试核心叙事：需求五步法 + 竞品五步法 + 三级 AI 架构 + 评测/成本/合规 + 真机踩坑（RN fetch vs XHR）
